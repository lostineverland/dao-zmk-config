manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned to a verified-good ZMK revision. This `stable` branch is a
      # frozen known-good build; `main` tracks the latest ZMK instead.
      revision: 9917e2f1371d5bb051bf8eb782c1297db41d742b
      import: app/west.yml
  self:
    path: config
