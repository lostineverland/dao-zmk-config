on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to match the verified-good ZMK revision in config/west.yml.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@9917e2f1371d5bb051bf8eb782c1297db41d742b
